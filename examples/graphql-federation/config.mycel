# GraphQL Federation Example Configuration
# This example demonstrates a federated GraphQL subgraph for products,
# with Federation v2 directives, entity resolvers, and subscriptions.

service {
  name    = "product-subgraph"
  version = "1.0.0"
}

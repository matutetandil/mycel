# Profiles Example Configuration
#
# This example demonstrates how to use connector profiles to:
# 1. Switch between multiple backends based on environment
# 2. Apply transforms to normalize data from different sources
# 3. Configure automatic fallback chains

service {
  name    = "pricing-service"
  version = "1.0.0"
}

# GraphQL Subscription Client Example
# A service that subscribes to an external GraphQL server's real-time events
# and stores them locally.

service {
  name    = "price-tracker"
  version = "1.0.0"
}
